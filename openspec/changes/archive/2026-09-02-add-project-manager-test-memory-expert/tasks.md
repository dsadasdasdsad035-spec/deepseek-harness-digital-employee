## 1. Template-backed memory initialization

- [x] 1.1 Extend the digital employee template schema and public types with validated, non-secret long-term memory seed declarations.
- [x] 1.2 Materialize a new template instance's memory seeds atomically with employee creation, preserving employee isolation, provenance, retention, and duplicate-free creation semantics.
- [x] 1.3 Add focused schema and file-provider tests for invalid seeds, successful initialization, two-instance isolation, and unchanged existing-instance behavior.

## 2. Project-manager template revision

- [x] 2.1 Preserve the existing `project-manager-test@1.0.0` template and register a new expert-enabled revision for new employee creation.
- [x] 2.2 Attach the Atlas memory seed to the new template revision and add the Risk Reviewer expert declaration with exact root and child delegation limits.
- [x] 2.3 Add the Risk Reviewer's package-owned instruction asset and restrict its skills, project evidence tools, MCP access, memory access, and descendant authority.
- [x] 2.4 Update package tests to assert the two template revisions, initialized Atlas memory, expert capability allowlist, and rejected expert descendant delegation.

## 3. Assembled Host and Web behavior

- [x] 3.1 Register all resources required by the new template revision in the Web development bundle without removing the existing revision.
- [x] 3.2 Extend Host and Web composition tests so template listing, employee creation, memory inspection, expert discovery, and expert delegation use the new revision.
- [x] 3.3 Add or update a keyless runnable snapshot that records initialized memory, delegated risk review, scoped MCP use, and the final root result.

## 4. Documentation and verification

- [x] 4.1 Update the project-manager package README pair and owning Agent Note pair with the automatic seed and Risk Reviewer behavior, then re-record translation metadata.
- [x] 4.2 Run focused unit, provider, package, assembled snapshot, typecheck, Web composition, and documentation checks.
