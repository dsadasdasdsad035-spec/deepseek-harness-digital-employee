# Tasks: company-persistent-positions

- [x] 1.1 Store `dsh-company-file/world-state` (cars/employees, lock+atomic merge) + unit tests
- [x] 1.2 Wire types + `companies` gateway remotes (read/report) + gateway tests + typert regen
- [x] 2.1 Client: campus car restore-by-id with validation; floor NPC position restore with registry probe fallback; snapshot accessor
- [x] 2.2 Client: 5 s reporter + close-time final report; world state loaded with companies/floor payloads
- [x] 3.1 Snapshot rail: read-empty → report → read-back acceptance; fixture world-state path redirect
- [x] 4.1 Gates + catalogs + READMEs + Agent Note + validate + commit/archive/push
