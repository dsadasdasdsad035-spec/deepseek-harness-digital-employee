## 1. Project Manager Reference Package

- [x] 1.1 Add a dedicated `project-manager-test` example package with versioned template metadata, package-owned `AGENTS.md`, project-manager personality, and documentation that identifies its test-only scope.
- [x] 1.2 Add deterministic project-planning, risk-review, and status-reporting skill fixtures plus bounded project memory seed data owned by the example package.
- [x] 1.3 Register the template's explicit skill, tool, MCP, memory, and instruction references through the existing digital employee template interfaces; add invariant coverage for resolvable package-owned references.

## 2. Deterministic Capability Providers

- [x] 2.1 Add test-only project-board and project-document tool providers with small structured fixture data and authorization-aware registration.
- [x] 2.2 Add a package-owned mock MCP project-data client that exposes deterministic milestones, owners, and risks without network access, credentials, or secret values.
- [x] 2.3 Compose the template, tool providers, mock MCP client, and memory fixture into an isolated digital employee test composition without changing generic capability or memory behavior.

## 3. Verification

- [x] 3.1 Add focused template and capability-composition tests proving the resolved employee exposes exactly its declared skills, tools, MCP client, instructions, and memory access.
- [x] 3.2 Add memory lifecycle tests proving the fixture projects relevant seed memory, records durable project-decision proposals through the existing policy, and preserves employee isolation.
- [x] 3.3 Add a keyless assembled workflow test that starts an isolated project-manager employee, exercises project planning, tool use, MCP data reads, risk reporting, and durable attribution.
- [x] 3.4 Add or update portable snapshot or accessible-output coverage for the assembled workflow without environment-specific normalization.
- [x] 3.5 Run focused package, composition, memory, and assembled workflow tests; run relevant typecheck, documentation, and OpenSpec strict validation commands.
