/**
 * Optional headless profile bundle extension for digital employee tasks.
 *
 * The package's runtime content is `cordis.patch.yml`; this module carries no
 * service of its own.
 * @module @deepseek-ai/dsh-headless-employee
 */

export {}
