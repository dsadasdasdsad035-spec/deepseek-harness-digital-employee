/**
 * Optional Web profile bundle extension for digital employee management.
 *
 * The package's runtime content is `cordis.patch.yml`; this module carries no
 * service of its own.
 * @module @deepseek-ai/dsh-digital-employee-suite
 */

export {}
