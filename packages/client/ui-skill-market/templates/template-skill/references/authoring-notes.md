# Authoring Notes

Reference files are optional. Use them for durable material that helps an agent
perform the skill without crowding the main `SKILL.md` instructions.

For example, a document-processing skill might keep its supported file
conventions here. Keep reference content readable, local to the package, and
free of executable code.
